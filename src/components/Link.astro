---
const { href, ...attrs } = Astro.props;
const isExternal = href?.startsWith('http://') || href?.startsWith('https://');
---

<a href={href} {...isExternal && { target: '_blank', rel: 'noopener noreferrer' }} {...attrs}><slot /></a>
